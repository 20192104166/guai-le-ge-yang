# 2026-06-02 开发日志

**📅 当前阶段**：Phase 0 - 项目筹备（**收官日**）
**👤 协作者**：用户 / Claude
**🎯 今日主题**：Git/远程仓库 · Unity 工程 · 字节插件 · WebGL 构建验证

---

## ✅ 今日完成

### 上午：Git 远程仓库 + 网络问题排查
- [x] **用户操作**：在 GitHub 创建私有仓库 `https://github.com/20192104166/guai-le-ge-yang.git`
- [x] **T0.17 Git 初始化**
  - `git init -b main` + `git lfs install`
  - 撰写 `.gitignore`（Unity 模板 + 抖音构建产物 + 后端 + Claude 本地配置）
  - 撰写 `.gitattributes`（行尾规范化 + LFS 规则 30+ 类型）
  - 撰写 `README.md`（GitHub 仓库门户，含徽章 + 文档索引）
- [x] **首次 commit (22070b4)** init: 29 个文件（纯文档 + 配置）
  - 小插曲：用户旧 Unity 项目目录 `Unityproject/My project/` 名称带空格，用户回滚删除后 Claude 用 `git commit --amend` 重写为干净 init commit
- [x] **GitHub push 网络问题排查**
  - 错误 1：`schannel: failed to receive handshake, SSL/TLS connection failed`（GitHub 无 VPN 访问被阻断）
  - 用户开启 VPN 后 curl 仍失败（VPN 仅代理浏览器，未覆盖命令行）
  - 错误 2：`fatal: Missing 'state' in response` + `could not read Username for 'https://github.com'`（GCM 凭据弹窗 Claude 终端无法响应）
  - **解决方案**：用户在自己 PowerShell 终端执行 `git push -u origin main`，浏览器弹 OAuth 授权后凭据缓存到 Windows
  - 之后 Claude 在 bash 工具里 push 自动使用缓存凭据 ✅

### 中午：Unity 项目骨架与路径折腾
- [x] **T0.18 Unity 2D 项目骨架**
  - 用户在 Unity Hub 创建 `UnityProject/GuaiLeGeYang/`（路径规范化无空格）
  - Unity 2022.3.62f3c1 LTS + 2D Feature Set 2.0.1
  - 默认场景 `Assets/Scenes/SampleScene.unity`
- [x] **Commit 6b15676** feat(unity): 添加 Unity 2D 项目骨架（27 个文件）
- [x] **T0.21 项目结构规范** v1.0
  - 仓库根目录结构 + 命名约定
  - Unity Assets/ 内部组织（`_Project/` 下划线置顶 + 职责分目录）
  - C# 命名空间规则（`GuaiLeGeYang.*` 与目录一一对应）
  - Assembly Definition 划分建议
  - 资源命名规范（snake_case + 类型前缀）
  - CloudFunctions/ 后端目录预留
  - Phase 1 启动目录创建优先级清单
- [x] **Commit 0ecb737** docs(structure): 项目结构规范 v1.0 + 日志/任务拆分同步

### 下午：T0.19 字节抖音转换插件 + WebGL 构建（折腾）
- [x] **用户安装字节插件**：从抖音开放平台下载并导入到 Unity
  - 装入 `Assets/Plugins/ByteGame/`（com.bytedance.bgdt + starksdk）
- [x] **首次构建失败（编译错误）**
  - Unity 弹出"Enter Safe Mode?"提示 → 项目有 C# 编译错误
  - Console 关键错误：`UnityException: Build target 'WebGL' not supported`
  - **根因**：WebGL Build Support 模块未装
- [x] **用户装 WebGL Build Support 模块**（Unity Hub → Installs → 2022.3.62f3 → Add Modules）
- [x] **路径折腾过程**
  - 用户为避免中文路径风险，把 Unity 项目临时挪到 `D:\UnityProjecet\GuaiLeGeYang`（脱离 Git）
  - 注意：路径拼写错误 `UnityProjec` + `et`（多了一个 e）
  - Claude bash/PowerShell 工具的 cwd 强制锁定 `D:\拐了个羊\`，无法自动迁移到 `D:\GuaiLeGeYang\`（"Device or resource busy" / "system path protection"）
  - **折中方案**：用户手动用 PowerShell 把 Unity 项目挪回 `D:\拐了个羊\UnityProject\GuaiLeGeYang\`（恢复 Git 统一管控）
  - 中文路径风险待后续验证：如果 WebGL 构建持续工作则保留中文路径；否则未来再整体迁移到 `D:\GuaiLeGeYang\`
- [x] **WebGL 构建首次成功** ✅
  - Console 0 errors / 0 warnings
  - 抖音开发者工具能加载构建产物，模拟器显示"Mini Program"加载圈（SampleScene 无渲染对象，白屏正常）
- [x] **.gitignore 补充**
  - 排除字节构建产物：`tt-minigame/` / `log` / `log.txt` / `game.json` / `project.config.json`
  - 排除 `.bgdt/` 临时文件
- [x] **Commit 619be71** feat(unity): 装入字节抖音 Unity 转换插件 + 路径迁移修复（172 个文件）
- [x] **Push 成功 → GitHub**
  - `0ecb737..619be71  main -> main`
  - LFS 14 个对象 3.1MB

---

## 📊 今日交付物

```
4 个 commit 推送到 GitHub：
  22070b4 init: 拐了个羊项目初始化（Phase 0 完成）
  6b15676 feat(unity): 添加 Unity 2D 项目骨架 (T0.18)
  0ecb737 docs(structure): 项目结构规范 v1.0 + 日志/任务拆分同步 (T0.21)
  619be71 feat(unity): 装入字节抖音 Unity 转换插件 + 路径迁移修复 (T0.19 完成)

Git LFS：14 个二进制对象（字节插件 DLL + PNG + JSON）

工程脚手架：
  ✅ Git 仓库初始化 + LFS
  ✅ .gitignore + .gitattributes
  ✅ README.md
  ✅ Unity 2D 项目（含字节抖音插件）
  ✅ WebGL Build Support 模块
  ✅ 首次 WebGL 构建成功
  ✅ 抖音开发者工具能加载小游戏
```

---

## 🚧 进行中
- 无（Phase 0 已 100% 完成 🎉）

---

## 📌 明日 / 下次会话待办

### Phase 1 - 物理 POC 启动
按 [`../docs/05-执行计划/任务拆分.md`](../docs/05-执行计划/任务拆分.md) Phase 1 段，28 项细化任务，分 6 组：

#### P1A 工程准备
- [x] T1.1 Unity 2022 LTS 已装 ✅（Phase 0 完成）
- [x] T1.2 字节抖音转换工具已装 ✅（T0.19 完成）
- [ ] **T1.3 创建 ShagaiPOC 测试场景**
- [ ] **T1.4 引入 DOTween 包**
- [ ] **T1.5 配置 Physics 2D 项目设置**（参考 [`物理引擎方案.md`](../docs/02-技术文档/物理引擎方案.md) §1.1）
- [ ] **T1.6 配置 FeltMat 物理材质**

#### P1B 美术占位
- [ ] T1.7-T1.9 绘制 4 张羊拐贴图 + 毡毯背景 + 粒子贴图

#### P1C 物理与判定核心
- [ ] T1.10-T1.15 ShagaiOrientation.cs / WeightedRandom.cs / FaceConvergence.cs / 接口 / 侧立重抛

#### P1D 抛掷交互与多枚
- [ ] T1.16-T1.19

#### P1E 统计验证
- [ ] T1.20-T1.23

#### P1F 抖音真机验证
- [ ] T1.24-T1.28

---

## ❓ 待确认问题
- 无阻塞型待确认事项
- 次级：DOTween 装入方式（Asset Store / Package Manager git URL）由用户在 Unity Editor 中选择

---

## 💡 决策记录

1. **Git 凭据缓存策略**：用户在自己终端首次 push 弹浏览器 OAuth，凭据自动缓存到 Windows Credential Manager。之后 Claude bash 工具继承此凭据，可直接 push。✅
2. **路径策略（暂定）**：保留中文根目录 `D:\拐了个羊\`，Unity 项目以英文子路径 `UnityProject\GuaiLeGeYang\` 存放。WebGL 构建实测无影响。若后续真机测试发现中文路径问题，再整体迁移到 `D:\GuaiLeGeYang\`。
3. **抖音构建产物不入 Git**：`tt-minigame/` / `log` / `game.json` / `project.config.json` 均为每次构建重新生成的产物，加入 `.gitignore` 排除，避免仓库膨胀。
4. **Phase 0 完整收官，进入 Phase 1**：所有 21 项 Phase 0 任务 100% 完成。下一阶段进入物理 POC 实施。

---

## ⚠️ 风险与阻塞

| 风险 | 影响 | 应对 |
|---|---|---|
| 中文路径可能导致 Unity 构建链 / 字节插件链路某些环节失败 | 后期发现需要整体迁移到英文路径 | 当前 WebGL 构建成功，先观察；如真机测试出现路径相关错误，则迁移到 `D:\GuaiLeGeYang\` |
| Unity Library/ 缓存占 1.8GB+ 磁盘 | 影响项目体积感受 | 已 gitignore；用户磁盘空间充足 |
| Claude 工具的 cwd 锁定限制 | 某些重型文件操作需用户介入 | 已掌握模式：路径迁移、git push 凭据等用户操作即可 |
| 异步 PvP 录像验证 + 抖音云开发额度 | 未来 Phase 1.5/2 实测时验证 | Phase 1 不依赖此 |

---

## 📝 备注

### Phase 0 全程总结（2026-05-15 ~ 2026-06-02，约 18 天）
- 总耗时：约 18 天（含决策沟通、文档迭代、工程脚手架）
- 总 commit：4 个（仅 Phase 0 期间）
- 总文档：26 份 markdown（v1.0 定稿 7 份）
- 总代码：0 行业务代码（Phase 0 不写代码，纯设计与脚手架）
- 总文件：约 200 个（含字节插件 4.6MB）

### 关键经验沉淀
- **决策前置**：花一周时间把 17 项关键决策定下来，避免后期返工
- **物理风险提前验证**：Phase 1 物理 POC 是全项目最大技术风险，必须最早跑通
- **路径规范严格**：Unity + 字节插件链路对路径敏感，从开始就用英文命名规范
- **Git 凭据缓存**：在 Windows 上首次 push 触发 GCM OAuth，之后 Claude 自动可用
- **小步快跑**：每次推进 1-2 个任务，每个有可验证产出，避免一口气大改

### 资料备注
- 字节抖音 Unity 转换插件官方下载：抖音开放平台 → 文档 → 小游戏 → 引擎接入 → Unity
- 用户的 AppId：`tt118b67368fefbb3807`
- GitHub 仓库地址：`https://github.com/20192104166/guai-le-ge-yang.git`
